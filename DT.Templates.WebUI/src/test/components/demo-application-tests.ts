/// <reference path="../../../src/jasmine.d.ts" />

import ko = require("knockout");
import target = require("components/demo-application/demo-application");

describe('demo-application component', () => {

	beforeEach(function () { 
		// TODO: put any setup work here 
	});

    //it('should take parameters', () => {
		
	//	var args = { data: ko.observable("value") };

	//	var instance = new target.viewModel();

	//	expect(instance.greeting()).toBe("value");
    //});

	// TODO: add more unit tests

});
